# SustainAI Environment Variables
# Copy to .env and fill in values as needed.

# GCP / Vertex AI (required for live agent mode only)
GCP_PROJECT=
VERTEX_LOCATION=us-central1
GEMINI_MODEL=gemini-2.5-flash

# Agent mode: "live" or "stub"
AGENT_MODE=stub

# Global random seed
SUSTAINAI_SEED=42
